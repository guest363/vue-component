// Импортируем Vue и тестируемый компонент
import Vue from 'vue'
import colorPicker from '../color-piсker/color-picker.vue';

describe(`colorPicker`, () => {
  // Проверка опций компонента
/*   it('has a created hook', () => {
    expect(typeof colorPicker.created).toBe('function')
  }) */

/*   // Оценка результатов методов в опциях
  it('sets the correct default data', () => {
    expect(typeof colorPicker.data).toBe('function')
    const defaultData = colorPicker.data()
    expect(defaultData.color).toBe(["black", "grey", "saddlebrown", "whitesmoke", "navajowhite"])
  }) */

/*   // Анализ экземпляра компонента при монтировании
  it('correctly sets the message when created', () => {
    const vm = new Vue(MyComponent).$mount()
    expect(vm.message).toBe('bye!')
  })

  // Монтирование экземпляра и оценка вывода рендеринга
  it('renders the correct message', () => {
    const Constructor = Vue.extend(MyComponent)
    const vm = new Constructor().$mount()
    expect(vm.$el.textContent).toBe('bye!')
  }) */
})